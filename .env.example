# Скопируйте в .env.local (в git не попадает) или задайте в Timeweb → Переменные.

# Куда приходят заявки (FormSubmit fallback / подсказка в ошибках)
MAIL_TO=Bekoev_2003@mail.ru

# Продакшен (Timeweb, output: export) — ключ Web3Forms:
# https://web3forms.com → форма → почта Bekoev_2003@mail.ru → Access Key
# Timeweb → NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY
# Локально: .env.local или .env.production перед npm run build
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
